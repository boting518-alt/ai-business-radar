# Intelligence Translation — zh-CN — v003

Translate exactly the requested canonical intelligence fields into faithful, concise, professional
Simplified Chinese. Return every requested `field_name` once and no extra fields.

Rules:

1. Preserve complete meaning, attribution, uncertainty, numbers, currencies, URLs, proper names,
   and evidence strength. Never turn `claims`, `may`, `can`, `is positioned as`, or `is presented
   as` into verified fact.
2. Write natural Chinese information order. Avoid English passive calques such as `被定位为可` or
   `被描述为可`, and avoid filler such as repeated `进行`, `实现`, `相关`, or `内部`. When attribution
   permits, prefer `用于`, `主要用于`, `可`, `能够`, `面向`, or `帮助`; do not delete attribution to
   obtain fluency.
3. Translate `creator` as `创作者` or contextually `发布者`, never `创建者`.
4. In legal context, translate `retainer` according to the sentence as `预付律师费`, `律师聘用金`,
   or `聘用费`; do not reduce it to a generic prepayment. Translate `intake` as `客户接待`,
   `案件受理`, or `信息采集` according to the workflow, and `prospective client` as `潜在客户`.
5. Translate business terms contextually: human `handoff` as `转交人工` or `人工接管`;
   `escalation` as `升级处理`; sales `lead` as `线索` but a person as `潜在客户`; dental `recall` as
   `复诊召回`, `患者召回`, or `复诊提醒`; `invoice extraction` as `发票数据提取`; and `month-end
   close` as `月末结账`.
6. Preserve established names such as QuickBooks, Shopify, VitalDesk, OpenAI, n8n, Vapi, and Make.
   Keep English technical terms when translation would be misleading.
7. Evidence remains a presentation translation, not independent verification. Do not add domain
   facts, outcomes, or interpretations.
8. Put preserved proper/technical terms in `preserved_terms` and material ambiguity in `warnings`;
   otherwise use empty lists.

Return only the strict translation output defined by the schema.

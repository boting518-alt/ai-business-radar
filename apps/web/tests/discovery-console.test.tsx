import {render,screen} from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import {describe,expect,it,vi} from "vitest";
import {DiscoveryConsole} from "@/components/discovery/discovery-console";

vi.mock("@/lib/auth/client",()=>({createClient:()=>({auth:{getSession:async()=>({data:{session:{access_token:"token"}}})}})}));
const topic={id:"topic-1",name:"AI Dental Front Desk",description:"Dental research",status:"paused",default_schedule:"manual",default_max_videos:5,default_max_pages:1,default_max_comments_per_video:10,query_count:2,last_run_at:null,next_run_at:null,latest_run:{id:"run-1",topic_id:"topic-1",query_id:"query-1",query_text:"AI dental receptionist",trigger_type:"manual",status:"completed",started_at:"2026-09-08T00:00:00Z",completed_at:"2026-09-08T00:01:00Z",videos_discovered:3,quota_estimate:100,error_message_safe:null,worker_message_id:"message-1"}};
describe("Discovery Console",()=>{it("renders status, topics, multiple-query editor and Run Now",async()=>{vi.stubGlobal("fetch",vi.fn().mockImplementation((input:RequestInfo|URL)=>{const url=String(input);const body=url.endsWith("system-status")?{redis:"configured",worker:"unknown",scheduler:"configured",youtube_api:"configured",openai_api:"configured"}:[topic];return Promise.resolve(new Response(JSON.stringify(body),{status:200,headers:{"Content-Type":"application/json"}}))}));render(<DiscoveryConsole/>);expect(screen.getByLabelText("正在加载数据发现")).toBeInTheDocument();expect(await screen.findByText("AI Dental Front Desk")).toBeInTheDocument();expect(screen.getByText(/completed · quota 100/)).toBeInTheDocument();await userEvent.click(screen.getByRole("button",{name:"+ Add query"}));expect(screen.getByLabelText("Query 2")).toBeInTheDocument();await userEvent.click(screen.getByRole("button",{name:"Run Now"}));expect(fetch).toHaveBeenCalledWith(expect.stringContaining("/topics/topic-1/run"),expect.objectContaining({method:"POST"}))})});

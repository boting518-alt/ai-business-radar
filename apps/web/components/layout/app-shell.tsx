import Link from "next/link";
import { BarChart3, BriefcaseBusiness, Eye, Radio, Star } from "lucide-react";
import type { CurrentUser } from "@/lib/api/types";
import { LogoutButton } from "@/components/navigation/logout-button";

const links=[["/radar","Radar",BarChart3],["/opportunities","机会",BriefcaseBusiness],["/signals","信号",Radio],["/watchlist","关注",Star]] as const;
export function AppShell({user,children}:{user:CurrentUser;children:React.ReactNode}){return <div className="min-h-screen bg-slate-50 text-slate-950 dark:bg-slate-950 dark:text-slate-100"><aside className="fixed inset-y-0 hidden w-60 border-r bg-white p-5 md:block dark:bg-slate-950"><Link href="/radar" className="font-semibold tracking-tight">AI Business Radar</Link><p className="mt-1 text-xs text-slate-500">YouTube intelligence terminal</p><nav aria-label="主导航" className="mt-8 space-y-1">{links.map(([href,label,Icon])=><Link key={href} href={href} className="flex items-center gap-3 rounded-md px-3 py-2 text-sm hover:bg-slate-100 dark:hover:bg-slate-900"><Icon size={16}/>{label}</Link>)}{user.role==="admin"&&<Link href="/admin/review" className="flex items-center gap-3 rounded-md px-3 py-2 text-sm hover:bg-slate-100 dark:hover:bg-slate-900"><Eye size={16}/>审核</Link>}</nav></aside><div className="md:pl-60"><header className="flex h-16 items-center justify-between border-b bg-white px-5 dark:bg-slate-950"><span className="text-sm text-slate-500">Intelligence Workspace</span><div className="flex items-center gap-3 text-sm"><span>{user.email??"已登录用户"} · {user.role}</span><LogoutButton/></div></header><main className="mx-auto max-w-7xl space-y-6 p-5 md:p-8">{children}</main></div></div>}

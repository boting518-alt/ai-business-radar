-- Reference seed is installed by migration 0017 so every environment is deterministic.
-- This file intentionally documents the seed identity; edit by creating taxonomy-v002, never in place.
SELECT 'taxonomy-v001' AS taxonomy_version;
